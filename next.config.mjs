/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatar.vercel.sh",
      "storage.googleapis.com",
      "ibb.co",
"      images.unsplash.com"    ],
  },
};

export default nextConfig;
